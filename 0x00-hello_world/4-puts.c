#include<stdio.h>
 /**
  * main - prints the phrase "programming is like building a multilingual puzzle
  * return : 0 if done the right way, otherwise non zero
  */
int main(void)
{
	puts("\"programming is like building a multilingual puzzle")
		return (0);
}
