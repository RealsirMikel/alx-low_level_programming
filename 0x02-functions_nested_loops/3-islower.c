#include <stdlib.h>
#include <stdio.h>
#include "main.h"

/**
 * _islower - chcks for lower case character
 *
 * Return: Always 0 (Success)
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
